# --- OS/Editor ---
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*.swo
*.tmp
.history/

# --- Node / package managers ---
node_modules/
.pnpm-store/
.npm/
.yarn/*
!.yarn/patches
!.yarn/releases
!.yarn/plugins
!.yarn/sdks
!.yarn/versions
.yarn-integrity
package-lock.json.lock
yarn.lock.lock
pnpm-lock.yaml.lock

# --- Logs & PIDs ---
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
pnpm-error.log*
pid/
pids/
*.pid
*.seed
*.pid.lock

# --- Env (mantén un .env.example) ---
.env
.env.*.local
.env.local
.env.* 
!.env.example

# --- TypeScript & build metadata ---
*.tsbuildinfo
.cache/
.eslintcache

# --- Coverage / testing ---
coverage/
.nyc_output/
jest-html-reporters-*.html
test-results/

# --- Dist / build outputs (server y cliente) ---
dist/
build/
**/.vite
**/dist
**/build

# (Si quieres servir la SPA con Nest desde /web/dist, recuerda no commitear estos artefactos)

# --- Vite ---
.vite/
vite.svg

# --- NestJS (server) ---
server/dist/
server/.cache/

# --- React/Vite app (cliente) ---
web/node_modules/
web/dist/
web/.vite/
web/.cache/

# En caso de que generes el cliente con otro nombre (p.ej. my-app):
my-app/node_modules/
my-app/dist/
my-app/.vite/
my-app/.cache/

# --- Misc. tools ---
.turbo/
.vercel/
.netlify/
